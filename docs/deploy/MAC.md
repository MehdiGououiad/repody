# macOS

Same Hub flow as Windows/Linux — see **[LOCAL.md](./LOCAL.md)**.

```bash
pnpm platform setup   # once
pnpm platform         # pull images + start
```

On Apple Silicon, host models use Metal (`llama-server` via Homebrew). PP-OCR auto-installs on first start.
