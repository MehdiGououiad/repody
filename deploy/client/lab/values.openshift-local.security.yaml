# OpenShift Local bundled lab — restore enterprise hardening after deploy/values/openshift.yaml.
# openshift.yaml disables networkPolicy for the external-profile smoke path; bundled lab re-enables it.

networkPolicy:
  enabled: true

observability:
  otelEnabled: false

config:
  operatorActionsEnabled: false
