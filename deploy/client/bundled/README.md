# Bundled values overlay

**Full bundled install guide:** [docs/deploy/CLIENT.md#bundled-profile-in-cluster-data-plane](../../../docs/deploy/CLIENT.md#bundled-profile-in-cluster-data-plane)

This directory contains `values.data.yaml` for the `repody-data` chart — use with `values-bundled.example.yaml` per the client guide.
