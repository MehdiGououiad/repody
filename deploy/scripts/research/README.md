# Research scripts (not product)

Experimental tooling. Safe to ignore for production.

| Script | Purpose |
|--------|---------|
| `qwen35-serve.mjs` | Qwen3.5-4B text→JSON arm (`pnpm qwen35:serve`) |

Config: `deploy/research/qwen35/`  
Backend benches: `backend/scripts/research/`
