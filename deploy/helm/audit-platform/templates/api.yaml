apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "audit-platform.fullname" . }}-api
  labels:
    {{- include "audit-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: control
spec:
  replicas: {{ .Values.api.replicas }}
  selector:
    matchLabels:
      {{- include "audit-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: control
  template:
    metadata:
      labels:
        {{- include "audit-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: control
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if and .Values.hatchet.enabled (not .Values.hatchet.clientToken) .Values.hatchet.bootstrapToken }}
      initContainers:
        {{- include "audit-platform.waitHatchetTokenInit" . | nindent 8 }}
      volumes:
        - name: hatchet-client-token
          secret:
            secretName: {{ include "audit-platform.fullname" . }}-hatchet-token
            optional: true
      {{- end }}
      containers:
        - name: api
          image: "{{ .Values.images.api.repository }}:{{ .Values.images.api.tag }}"
          imagePullPolicy: {{ .Values.images.api.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: {{ include "audit-platform.fullname" . }}-config
          env:
            - name: AUDIT_OTEL_SERVICE_NAME
              value: audit-workbench-api
            {{- include "audit-platform.dataPlaneEnv" . | nindent 12 }}
            - name: AUDIT_ADMIN_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "audit-platform.fullname" . }}-secrets
                  key: AUDIT_ADMIN_API_TOKEN
            {{- if .Values.observability.sentryDsn }}
            - name: SENTRY_DSN
              value: {{ .Values.observability.sentryDsn | quote }}
            - name: SENTRY_ENABLE_LOGS
              value: "true"
            {{- end }}
          readinessProbe:
            httpGet:
              path: /v1/healthz/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /v1/healthz/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            {{- toYaml .Values.api.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "audit-platform.fullname" . }}-api
  labels:
    {{- include "audit-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: control
spec:
  type: ClusterIP
  selector:
    {{- include "audit-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: control
  ports:
    - name: http
      port: 8000
      targetPort: http
