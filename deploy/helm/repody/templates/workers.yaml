apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "repody.fullname" . }}-worker-ocr
  labels:
    {{- include "repody.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker-ocr
spec:
  replicas: {{ .Values.workerOcr.replicas }}
  selector:
    matchLabels:
      {{- include "repody.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker-ocr
  template:
    metadata:
      labels:
        {{- include "repody.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker-ocr
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: {{ .Values.workerOcr.terminationGracePeriodSeconds }}
      {{- if .Values.workerOcr.topologySpreadConstraints.enabled }}
      topologySpreadConstraints:
        - maxSkew: {{ .Values.workerOcr.topologySpreadConstraints.maxSkew }}
          topologyKey: {{ .Values.workerOcr.topologySpreadConstraints.topologyKey | quote }}
          whenUnsatisfiable: {{ .Values.workerOcr.topologySpreadConstraints.whenUnsatisfiable }}
          labelSelector:
            matchLabels:
              {{- include "repody.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: worker-ocr
      {{- end }}
      securityContext:
        {{- include "repody.podSecurityContext" . | nindent 8 }}
      {{- include "repody.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: worker
          image: {{ include "repody.backendImage" . | quote }}
          imagePullPolicy: {{ include "repody.backendImagePullPolicy" . }}
          command:
            - python
            - -m
            - audit_workbench.taskiq.worker
          securityContext:
            {{- include "repody.containerSecurityContext" . | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "repody.fullname" . }}-config
          env:
            - name: AUDIT_RUN_MIGRATIONS_ON_STARTUP
              value: "false"
            - name: AUDIT_WORKER_POOL
              value: {{ .Values.workerOcr.pool | quote }}
            - name: AUDIT_WORKER_OCR_MAX_JOBS
              value: {{ .Values.workerOcr.maxJobs | quote }}
            - name: AUDIT_REPODY_VLM_WARMUP_ON_START
              value: {{ .Values.workerOcr.warmupOnStart | quote }}
            - name: AUDIT_OTEL_SERVICE_NAME
              value: repody-worker-ocr
            {{- include "repody.vllmSecretEnv" . | nindent 12 }}
            {{- include "repody.dataPlaneEnv" . | nindent 12 }}
            {{- include "repody.bugsinkEnv" . | nindent 12 }}
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - python -c "import pathlib,sys; needle='audit_workbench.taskiq.worker'; sys.exit(0 if any(needle in p.read_text(errors='ignore').replace('\\x00',' ') for p in pathlib.Path('/proc').glob('[0-9]*/cmdline')) else 1)"
            initialDelaySeconds: 45
            periodSeconds: 30
            failureThreshold: 3
          resources:
            {{- toYaml .Values.workerOcr.resources | nindent 12 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "repody.fullname" . }}-worker-fast
  labels:
    {{- include "repody.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker-fast
spec:
  replicas: {{ .Values.workerFast.replicas }}
  selector:
    matchLabels:
      {{- include "repody.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker-fast
  template:
    metadata:
      labels:
        {{- include "repody.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker-fast
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: {{ .Values.workerFast.terminationGracePeriodSeconds }}
      {{- if .Values.workerFast.topologySpreadConstraints.enabled }}
      topologySpreadConstraints:
        - maxSkew: {{ .Values.workerFast.topologySpreadConstraints.maxSkew }}
          topologyKey: {{ .Values.workerFast.topologySpreadConstraints.topologyKey | quote }}
          whenUnsatisfiable: {{ .Values.workerFast.topologySpreadConstraints.whenUnsatisfiable }}
          labelSelector:
            matchLabels:
              {{- include "repody.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: worker-fast
      {{- end }}
      securityContext:
        {{- include "repody.podSecurityContext" . | nindent 8 }}
      {{- include "repody.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: worker
          image: {{ include "repody.backendImage" . | quote }}
          imagePullPolicy: {{ include "repody.backendImagePullPolicy" . }}
          command:
            - python
            - -m
            - audit_workbench.taskiq.worker
          securityContext:
            {{- include "repody.containerSecurityContext" . | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "repody.fullname" . }}-config
          env:
            - name: AUDIT_RUN_MIGRATIONS_ON_STARTUP
              value: "false"
            - name: AUDIT_WORKER_POOL
              value: {{ .Values.workerFast.pool | quote }}
            - name: AUDIT_WORKER_FAST_MAX_JOBS
              value: {{ .Values.workerFast.maxJobs | quote }}
            - name: AUDIT_REPODY_VLM_WARMUP_ON_START
              value: {{ .Values.workerFast.warmupOnStart | quote }}
            - name: AUDIT_OTEL_SERVICE_NAME
              value: repody-worker-fast
            {{- include "repody.vllmSecretEnv" . | nindent 12 }}
            {{- include "repody.dataPlaneEnv" . | nindent 12 }}
            {{- include "repody.bugsinkEnv" . | nindent 12 }}
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - python -c "import pathlib,sys; needle='audit_workbench.taskiq.worker'; sys.exit(0 if any(needle in p.read_text(errors='ignore').replace('\\x00',' ') for p in pathlib.Path('/proc').glob('[0-9]*/cmdline')) else 1)"
            initialDelaySeconds: 45
            periodSeconds: 30
            failureThreshold: 3
          resources:
            {{- toYaml .Values.workerFast.resources | nindent 12 }}
