# Immutable image tags promoted by CI (gitops-promote-staging workflow).
# Do not edit by hand in staging — bump via CI after images are pushed.
images:
  backend:
    tag: "<git-sha>"
  api:
    tag: "<git-sha>"
  worker:
    tag: "<git-sha>"
  web:
    tag: "<git-sha>"
