apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-llamacpp.fullname" . }}
  labels:
    {{- include "inference-llamacpp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "inference-llamacpp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
